{
  "name": "@enterprise-brain/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@enterprise-brain/contracts": "workspace:*",
    "@enterprise-brain/database": "workspace:*",
    "@enterprise-brain/domain": "workspace:*",
    "fastify": "^5.0.0",
    "openai": "^7.8.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0"
  }
}
