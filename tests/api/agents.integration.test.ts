import { afterAll, beforeEach, describe, expect, it } from 'vitest';
import { createApp } from '../../apps/api/src/app.js';
import { createPrismaClient, encodePassword, hashSessionToken } from '../../packages/database/src/index.js';
const database=process.env.DATABASE_URL?createPrismaClient(process.env.DATABASE_URL):undefined; const db=()=>{if(!database)throw new Error('DATABASE_URL is required');return database;}; const now=new Date('2026-09-06T00:00:00.000Z');
async function clean(){const c=db();await c.auditEvent.deleteMany();await c.session.deleteMany();await c.account.deleteMany();await c.humanConfirmation.deleteMany();await c.review.deleteMany();await c.resultArtifact.deleteMany();await c.result.deleteMany();await c.artifact.deleteMany();await c.agentToolCall.deleteMany();await c.agentRun.deleteMany();await c.agentAssignment.deleteMany();await c.agentVersion.deleteMany();await c.agentDefinition.deleteMany();await c.taskDependency.deleteMany();await c.taskAssignment.deleteMany();await c.task.deleteMany();await c.projectMember.deleteMany();await c.project.deleteMany();await c.departmentMembership.deleteMany();await c.permissionOverride.deleteMany();await c.organizationMembership.deleteMany();await c.department.deleteMany();await c.organization.deleteMany();await c.user.deleteMany();}
async function token(userId:string){const raw=`token-${userId}-${'x'.repeat(40)}`;await db().account.create({data:{id:`account-${userId}`,userId,login:`${userId}@test.local`,passwordHash:await encodePassword('Password!2026'),status:'ACTIVE',createdAt:now,updatedAt:now}});await db().session.create({data:{id:`session-${userId}`,accountId:`account-${userId}`,tokenHash:hashSessionToken(raw),createdAt:now,expiresAt:new Date(now.getTime()+86400000)}});return{authorization:`Bearer ${raw}`};}
async function fixture(){const c=db();await c.organization.create({data:{id:'org',name:'Org',status:'ACTIVE',createdAt:now,updatedAt:now}});await c.user.createMany({data:['owner','employee'].map(id=>({id,name:id,systemRole:'EMPLOYEE' as const,createdAt:now,updatedAt:now}))});await c.organizationMembership.createMany({data:[{id:'om-owner',organizationId:'org',userId:'owner',role:'OWNER',status:'ACTIVE',createdAt:now,updatedAt:now},{id:'om-employee',organizationId:'org',userId:'employee',role:'MEMBER',status:'ACTIVE',createdAt:now,updatedAt:now}]});await c.department.create({data:{id:'dept',organizationId:'org',name:'Product',status:'ACTIVE',createdAt:now,updatedAt:now}});await c.departmentMembership.create({data:{id:'dm',organizationId:'org',departmentId:'dept',userId:'employee',role:'MEMBER',status:'ACTIVE',createdAt:now,updatedAt:now}});await c.project.create({data:{id:'project',name:'Project',status:'ACTIVE',createdAt:now,updatedAt:now}});await c.projectMember.create({data:{id:'pm',projectId:'project',userId:'employee',role:'MEMBER',createdAt:now,updatedAt:now}});await c.task.create({data:{id:'task',projectId:'project',title:'Task',priority:'P2',status:'TODO',acceptanceCriteria:[],createdAt:now,updatedAt:now}});}
async function agent(app:Awaited<ReturnType<typeof createApp>>,owner:Record<string,string>,profile:'READ_ONLY_WORK'|'CONFIRMED_WRITE_WORK'='READ_ONLY_WORK'){const r=await app.inject({method:'POST',url:'/agents',headers:owner,payload:{key:`agent-${profile}`,name:'Agent',runtimeProfile:profile}});expect(r.statusCode).toBe(200);return r.json().id as string;}
async function assign(app:Awaited<ReturnType<typeof createApp>>,owner:Record<string,string>,id:string){expect((await app.inject({method:'PUT',url:`/agents/${id}/assignments`,headers:owner,payload:{scopeType:'USER',scopeId:'employee'}})).statusCode).toBe(200);}
function run(app:Awaited<ReturnType<typeof createApp>>,headers:Record<string,string>,id:string,intent:Record<string,unknown>={name:'read_file',relativePath:'docs/a.md'}){return app.inject({method:'POST',url:'/tasks/task/agent-runs',headers,payload:{agentId:id,intent}});}
describe('Agent Catalog API',()=>{beforeEach(clean);afterAll(async()=>database?.$disconnect());
it('merges effective assignment sources and keeps catalog management session-scoped',async()=>{await fixture();const app=await createApp({prisma:db()}),owner=await token('owner'),employee=await token('employee'),id=await agent(app,owner);for(const body of [{scopeType:'ORGANIZATION',scopeId:'org'},{scopeType:'DEPARTMENT',scopeId:'dept'},{scopeType:'USER',scopeId:'employee'}])expect((await app.inject({method:'PUT',url:`/agents/${id}/assignments`,headers:owner,payload:body})).statusCode).toBe(200);expect((await app.inject({method:'GET',url:'/me/agents',headers:employee})).json().agents).toEqual([expect.objectContaining({id,assignmentSources:expect.arrayContaining(['ORGANIZATION','DEPARTMENT','USER'])})]);expect((await app.inject({method:'POST',url:'/agents',headers:employee,payload:{key:'bad',name:'Bad',runtimeProfile:'READ_ONLY_WORK'}})).statusCode).toBe(403);await app.close();});
it('creates only assigned supported catalog runs with server-owned provenance',async()=>{await fixture();const app=await createApp({prisma:db()}),owner=await token('owner'),employee=await token('employee'),id=await agent(app,owner);expect((await run(app,employee,id)).statusCode).toBe(403);expect(await db().agentRun.count()).toBe(0);await assign(app,owner,id);const created=await run(app,employee,id);expect(created.statusCode).toBe(201);expect(created.json().run).toMatchObject({agentDefinitionKey:'agent-READ_ONLY_WORK',agentVersion:1,status:'RUNNING'});expect(await db().agentToolCall.count()).toBe(1);const forged=await app.inject({method:'POST',url:'/tasks/task/agent-runs',headers:employee,payload:{agentId:id,agentDefinitionKey:'forged',agentVersion:99,runtimeProfile:'CONFIRMED_WRITE_WORK',allowedTools:['write_file'],userId:'owner',organizationId:'other',intent:{name:'read_file',relativePath:'docs/a.md'}}});expect(forged.statusCode).toBe(400);await app.close();});
it.each(['definition','version','assignment'] as const)('rejects disabled %s without writes',async(disabled)=>{await fixture();const app=await createApp({prisma:db()}),owner=await token('owner'),employee=await token('employee'),id=await agent(app,owner);await assign(app,owner,id);if(disabled==='definition')await db().agentDefinition.update({where:{id},data:{status:'DISABLED'}});if(disabled==='version')await db().agentVersion.updateMany({where:{agentDefinitionId:id},data:{status:'DISABLED'}});if(disabled==='assignment')await db().agentAssignment.updateMany({where:{agentDefinitionId:id},data:{status:'DISABLED'}});expect((await run(app,employee,id)).statusCode).toBe(403);expect(await db().agentRun.count()).toBe(0);expect(await db().agentToolCall.count()).toBe(0);await app.close();});
it('enforces runtime profiles and live assignment or permission revocation with one Session',async()=>{await fixture();const app=await createApp({prisma:db()}),owner=await token('owner'),employee=await token('employee'),reader=await agent(app,owner);await assign(app,owner,reader);expect((await run(app,employee,reader)).statusCode).toBe(201);const bad=await run(app,employee,reader,{name:'write_file',relativePath:'x.md',payloadSize:0,payloadSha256:'a'.repeat(64),effect:'CREATE',deviceId:'device'});expect(bad.statusCode).toBe(400);expect(bad.json().error.code).toBe('AGENT_TOOL_NOT_ALLOWED');const writer=await agent(app,owner,'CONFIRMED_WRITE_WORK');await assign(app,owner,writer);const write=await run(app,employee,writer,{name:'write_file',relativePath:'x.md',payloadSize:0,payloadSha256:'a'.repeat(64),effect:'CREATE',deviceId:'device'});expect(write.statusCode).toBe(201);expect(write.json().run.status).toBe('WAITING_HUMAN');const a=await db().agentAssignment.findFirstOrThrow({where:{agentDefinitionId:reader}});await db().agentAssignment.delete({where:{id:a.id}});const before=await db().agentRun.count();expect((await run(app,employee,reader)).statusCode).toBe(403);expect(await db().agentRun.count()).toBe(before);await db().permissionOverride.create({data:{id:'deny',organizationId:'org',userId:'employee',scopeType:'ORGANIZATION',scopeId:'org',resource:'AGENT',action:'EXECUTE',effect:'DENY',createdAt:now,updatedAt:now}});expect((await run(app,employee,writer,{name:'write_file',relativePath:'y.md',payloadSize:0,payloadSha256:'a'.repeat(64),effect:'CREATE',deviceId:'device'})).statusCode).toBe(403);await db().permissionOverride.delete({where:{id:'deny'}});expect((await run(app,employee,writer,{name:'write_file',relativePath:'z.md',payloadSize:0,payloadSha256:'a'.repeat(64),effect:'CREATE',deviceId:'device'})).statusCode).toBe(201);await app.close();});
});
